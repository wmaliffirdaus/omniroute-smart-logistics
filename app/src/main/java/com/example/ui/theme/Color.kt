package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// --- High Density Theme Palette ---
val HighDensityBg = Color(0xFFFDF8F6)          // Warm custom background
val HighDensityText = Color(0xFF1D1B1E)        // Charcoal text
val HighDensitySecondary = Color(0xFFF3EFF4)   // Very light purple-grey for Bar headers
val HighDensityBorder = Color(0xFFE7E0E8)      // Border outline color
val HighDensityPrimary = Color(0xFF6750A4)     // Main M3 deep purple

// Sync and API Telemetry Accents
val HighDensitySyncBg = Color(0xFFE8DEF8)      // Info Purple Background
val HighDensitySyncText = Color(0xFF21005D)    // Dark Purple Text
val HighDensitySyncBorder = Color(0xFFD0BCFF)  // Purple Border

val HighDensityApiBg = Color(0xFFD3E3FD)       // API blue Background
val HighDensityApiText = Color(0xFF041E49)     // Dark Blue Text
val HighDensityApiBorder = Color(0xFFA8C7FA)   // Blue Border

// Adapt existing variables to High Density Theme
val TechBlue = Color(0xFF0B57D0)               // Google Blue (active pointer, highlights)
val TechBlueDark = Color(0xFF003F9A)
val SlateBg = Color(0xFFFDF8F6)                // High Density bg replaces dark slate
val SlateSurface = Color(0xFFFFFFFF)           // White cards replace dark steel
val AccentOrange = Color(0xFFF97316)

val CleanBg = Color(0xFFFDF8F6)
val CleanSurface = Color(0xFFFFFFFF)

// Status & Helper Colors
val LogiGreen = Color(0xFF10B981)              // Live tracking green
val LogiAmber = Color(0xFFF59E0B)              // Warning
val LogiRed = Color(0xFFEF4444)                // Warning Red
val LogiGray = Color(0xFF49454F)               // Subtext grey
val OverlayWhite = Color(0x0F000000)           // Slight dark tint overlay
